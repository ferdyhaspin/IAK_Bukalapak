<resources>

    <!-- Base application theme. -->
    <style name="AppThemeFull" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/merah</item>
        <item name="colorPrimaryDark">@color/merahketat</item>
        <item name="colorAccent">@color/merah</item>
    </style>

    <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingstars</item>
        <item name="android:minHeight">22dip</item>
        <item name="android:maxHeight">22dip</item>
    </style>

    <style name="foodRatingBar_24dp" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingstarts_24dp</item>
        <item name="android:minHeight">22dip</item>
        <item name="android:maxHeight">22dip</item>
    </style>

</resources>
